# Región AWS principal para desplegar los stacks (us-east-1 por defecto)
AWS_REGION=us-east-1

# Prefijo o identificador del proyecto para nombrar recursos (usado como base para nombres de bucket, cluster, etc.)
PROJECT_NAME=MiProyecto

# Configuración de VPC y subredes
VPC_CIDR=10.0.0.0/16         # Rango CIDR de la VPC
MAX_AZS=2                    # Número de zonas de disponibilidad a usar (alta disponibilidad)
# Nota: Las subredes se crearán automáticamente: Public, Application (privada con NAT), Database (privada aislada)

# Dominio personalizado para el frontend (CloudFront) y ARN de certificado ACM (us-east-1) si se tiene.
# Dejar en blanco si no se usa dominio personalizado aún.
FRONTEND_DOMAIN=            # e.g., www.tusitio.com (debe estar registrado en Route53 u otro DNS)
FRONTEND_CERT_ARN=          # ARN del certificado ACM en us-east-1 para FRONTEND_DOMAIN

# (Opcional) Dominio personalizado para el ALB/API y su certificado ACM (en la misma región de la VPC, ej. us-east-1).
# Si se desea exponer el ALB vía un subdominio (ej. api.tusitio.com) en lugar de usarlo solo tras CloudFront.
BACKEND_DOMAIN=             # e.g., api.tusitio.com
BACKEND_CERT_ARN=           # ARN del cert. ACM regional para BACKEND_DOMAIN

# Configuración de Amazon RDS
DB_ENGINE=mysql             # Motor de base de datos (mysql, postgres, aurora, etc.)
DB_ENGINE_VERSION=8.0.28    # Versión del motor (ejemplo MySQL 8.0.x)
DB_INSTANCE_CLASS=db.t3.micro    # Clase de instancia para RDS (mínima para dev/proyecto)
DB_NAME=appdb               # Nombre de la base de datos principal
DB_USERNAME=admin           # Nombre de usuario administrador de la BD 
# La contraseña de la BD no se especifica aquí por seguridad; se generará aleatoriamente y almacenará en Secrets Manager.

# Configuración del repositorio de contenedor (ECR) y aplicación ECS
ECR_REPO_NAME=mi-proyecto-app     # Nombre del repositorio ECR para la imagen Docker del backend
ECS_DESIRED_COUNT=2               # Número deseado de tareas ECS Fargate
ECS_TASK_CPU=256                  # CPU por tarea (256 = 0.25 vCPU)
ECS_TASK_MEMORY=512               # Memoria (MB) por tarea
ECS_CONTAINER_PORT=80             # Puerto en el contenedor donde escucha la app (ej. 80 para HTTP)
CONTAINER_IMAGE_URI=             # URI de imagen Docker para desplegar (e.g., account-id.dkr.ecr.region.amazonaws.com/mi-proyecto-app:latest)
# Nota: Si CONTAINER_IMAGE_URI se deja vacío, se asumirá que la imagen será cargada manualmente al ECR creado antes de desplegar ECS.

# Configuración de Cognito (Autenticación de usuarios)
COGNITO_USER_POOL_NAME=MiProyectoUsers       # Nombre para el User Pool de Cognito
COGNITO_APP_CLIENT_NAME=MiProyectoWebClient  # Nombre para la aplicación cliente (frontend) de Cognito

# (Otros valores como IDs de VPC, SGs, ARNs de roles, etc., serán obtenidos dinámicamente por CDK, no es necesario especificarlos manualmente.)

